<section class="event">
  <%= render 'details' %>
  <%= render 'map' %>
  <%= render partial: 'participants', locals: {
    presentations: @presentations,
    postponed_presentations: @postponed_presentations,
    participants: @participants
  } %>
</section>
