%section{class: 'person'}
  %header
    = image_tag(avatar_url(@person, 80), alt: "Avatar not found")
    %h1
      = @person.full_name
  %section{class: 'presentations'}
    %h2
      Talks
    %ul
      - @person.presentations.done.each do |presentation|
        %li
          %strong
            = presentation.title
          on 
          = presentation.event_title
  %section{class: 'participations'}
    %h2
      Presence
    %p
      = @person.full_name
      was on 
      = @person.amount_of_participations
      meetups.
